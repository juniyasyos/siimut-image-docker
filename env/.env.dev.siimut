# ===========================================
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# ===========================================
# Use this for local development with custom login (NO SSO)
# ⚠️  DO NOT use this for production!

# Project Configuration
STACK_NAME=siimut
APP_DIR=siimut
HOST_IP=192.168.1.9

# Database Configuration - Development
DB_PORT=3306
MYSQL_ROOT_PASSWORD=secret123
MYSQL_DATABASE=siimut
MYSQL_USER=siimut_user
MYSQL_PASSWORD=siimut-password
MYSQL_RANDOM_ROOT_PASSWORD=no

# Web Server Configuration  
WEB_HTTP_PORT=8080
PMA_PORT=8081
REDIS_PORT=6379
VITE_PORT=3000

# PHP Configuration - Development
PHP_OPCACHE_VALIDATE_TIMESTAMPS=1
COMPOSER_CACHE_DIR=/tmp/composer
PHP_MEMORY_LIMIT=512M

# User/Group IDs (adjust to match host system)
UID=1000
GID=1000

# Build & Runtime Configuration - DEVELOPMENT MODE
APP_ENV=local
ENABLE_XDEBUG=false
RUN_MIGRATIONS=false

# Laravel Application Environment - Development
APP_NAME="SI-IMUT"
APP_KEY=base64:HEh3OnCEBkJY1dzwFQXa3St2h0JyM+w5ImcreuUybgo=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8088

# Database Connection
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=siimut_db
DB_USERNAME=siimut_user
DB_PASSWORD=siimut-password

# Redis Configuration
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0

# Cache & Queue Configuration - Development
CACHE_DRIVER=database
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Broadcasting
BROADCAST_DRIVER=log

# Mail Configuration (development)
MAIL_MAILER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@your-domain.com
MAIL_FROM_NAME="${APP_NAME}"

# Filesystem & S3 Storage (MinIO)
FILESYSTEM_DISK=s3

# MinIO S3 Configuration
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=password
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=siimut
AWS_ENDPOINT=http://minio:9000
AWS_URL=http://127.0.0.1:9000/siimut
AWS_USE_PATH_STYLE_ENDPOINT=true

# Logging - Development
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Security - Development
SESSION_SECURE_COOKIE=false
SESSION_HTTPONLY=true
SESSION_SAME_SITE=lax
FORCE_HTTPS=false
HSTS_PRELOAD=false

# phpMyAdmin Security
PMA_BLOWFISH_SECRET=passsword

# ===================================================
# Identity & Access Management (SSO Configuration)
# ===================================================
# DEVELOPMENT MODE: Custom login with NIP/Password (NO SSO)
USE_SSO=false
IAM_ENABLED=false

# IAM Server Configuration (for reference only, not used in dev)
IAM_HOST=http://127.0.0.1:8000
IAM_VERIFY_ENDPOINT=${IAM_HOST}/api/sso/verify
IAM_APP_KEY=siimut

# IAM SSO Client Package Configuration (inactive in dev)
IAM_JWT_SECRET=SIIMUT_ZK8FnRKJo5GlfBoN0izTVg0fR63r9UsgY86IaHeN
IAM_BASE_URL=${IAM_HOST}
IAM_LOGIN_ROUTE=/login
IAM_CALLBACK_ROUTE=/callback
IAM_DEFAULT_REDIRECT=/
IAM_GUARD=web
IAM_USER_MODEL=App\Models\User
IAM_IDENTIFIER_FIELD=nip
IAM_PRESERVE_SESSION_ID=true
IAM_SYNC_ROLES=true
IAM_ROLE_GUARD_NAME=web
IAM_STORE_TOKEN_IN_SESSION=true

# ===================================================
# DEVELOPMENT MODE CHECKLIST:
# ===================================================
# ✓ APP_ENV=local (single assignment!)
# ✓ APP_DEBUG=true (for debugging)
# ✓ LOG_LEVEL=debug (detailed logs)
# ✓ USE_SSO=false (custom login)
# ✓ CACHE_DRIVER=database (simple cache)
# ✓ PHP_OPCACHE_VALIDATE_TIMESTAMPS=1 (reload on change)
# ===================================================
