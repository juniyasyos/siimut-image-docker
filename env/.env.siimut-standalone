# ===========================================
# SIIMUT STANDALONE - No SSO Configuration
# ===========================================
# ⚠️  IMPORTANT: Change all default passwords and secrets before production deployment!

# Project Configuration
STACK_NAME=siimut-standalone
APP_DIR=siimut
HOST_IP=192.168.1.9

# Database Configuration
DB_PORT=3306
# ⚠️  CHANGE THESE PASSWORDS FOR PRODUCTION!
MYSQL_ROOT_PASSWORD=secret123
MYSQL_DATABASE=siimut
MYSQL_USER=siimut
MYSQL_PASSWORD=secret123
MYSQL_RANDOM_ROOT_PASSWORD=no

# Web Server Configuration  
SIIMUT_PORT=8000
PMA_PORT=8081
VITE_PORT=3000

# PHP Configuration - Production Optimized
PHP_OPCACHE_VALIDATE_TIMESTAMPS=0
COMPOSER_CACHE_DIR=/tmp/composer
PHP_MEMORY_LIMIT=512M

# User/Group IDs (adjust to match host system)
UID=1000
GID=1000

# Build Configuration
APP_ENV=production
ENABLE_XDEBUG=false
RUN_MIGRATIONS=false

# Laravel Application Environment - Production
APP_NAME="SIIMUT"
APP_ENV=local
APP_KEY=base64:yihVQm1MhFlOXgVZv1CbM/1JEH3MiSimKnG8MHifLls=
APP_DEBUG=true
APP_URL=http://192.168.1.9:8000

# Database Connection
DB_CONNECTION=mysql
DB_HOST=database-service
DB_PORT=3306
DB_DATABASE=siimut
DB_USERNAME=siimut
DB_PASSWORD=secret123

# Cache & Queue Configuration - Production Optimized
CACHE_DRIVER=file
QUEUE_CONNECTION=database
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Broadcasting
BROADCAST_DRIVER=log

# Mail Configuration (adjust for your mail service)
MAIL_MAILER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@your-domain.com
MAIL_FROM_NAME="${APP_NAME}"

# Filesystem & S3 Storage (not used in standalone mode, but keep configured)
FILESYSTEM_DISK=local

# Logging - Production
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# Security
SESSION_SECURE_COOKIE=false
SESSION_HTTPONLY=true
SESSION_SAME_SITE=lax

# ===================================================
# STANDALONE MODE - No SSO
# ===================================================
# Use custom local authentication only
USE_SSO=false
IAM_ENABLED=false

# Filament Authentication (Local)
FILAMENT_AUTH_METHOD=local

# ===================================================
# PRODUCTION DEPLOYMENT CHECKLIST:
# ===================================================
# □ Change all passwords and secrets above
# □ Generate new APP_KEY: php artisan key:generate
# □ Update APP_URL to your actual domain
# □ Configure mail settings
# □ Set up SSL certificates
# □ Configure backup strategy
# □ Set up monitoring and logging
# □ Test all functionality in staging first
# ===================================================
